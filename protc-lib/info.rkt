#lang info

(define collection 'multi)

(define deps '("base"
               "brag"
               "debug"  ; XXX remove when no longer needed!
               "parsack"
               "rosette"
               "syntax-warn"))
(define build-deps '())
