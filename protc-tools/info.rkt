#lang info

(define collection 'multi)

(define version "0.0.1")
(define deps '("protc-tools-lib"))
(define build-deps '())

(define implies deps)

(define pkg-desc "Protc language tooling")

(define pkg-authors '(tgbugs))
