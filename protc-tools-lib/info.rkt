#lang info

(define collection 'multi)

(define version "0.0.1")
(define deps '("base"
               "gui-lib"
               "protc"))
(define build-deps '())

(define implies deps)

(define pkg-desc "Code implementing Protc language tooling")

(define pkg-authors '(tgbugs))
